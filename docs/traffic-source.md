# 교통량 원천 확정 (2026-08-31)

## 결론

**`15043774` 한국도로공사_영업소별 교통량** — 이것이 우리가 찾던 데이터다.

https://www.data.go.kr/data/15043774/fileData.do

```
집계일자 · 영업소코드
TCS하이패스구분코드 (1: TCS, 2: 하이패스)
입출구구분코드     (0: 입구,  1: 출구)
고속도로운영기관구분코드 · 영업형태구분코드
1종·2종·3종·4종·5종·6종(경차) 교통량 · 총교통량

갱신: 수시 (자동)
포털 설명: "정기적으로 갱신되며 통계적 신뢰성과 시계열 비교가 가능하도록 구축"
```

## 왜 이것인가

가설이 **차종별 교통량 변화**를 다루므로 1~6종 구분이 필수인데 이 자료에 있다.
그리고 앞서 위험으로 짚었던 두 가지가 컬럼으로 해결된다.

- **하이패스 분리 문제** — TCS와 하이패스가 따로 집계돼 있어, 하이패스 전환으로
  TCS 교통량이 줄어든 것을 "교통량 감소"로 오독할 위험을 컬럼 하나로 막는다.
- **입출구 중복** — 입구/출구가 구분돼 있어 양방향을 이중계상하지 않는다.

## 받는 방법 — 오픈API 로 자동 수집 가능

파일데이터지만 공공데이터활용지원센터가 오픈API 로 자동변환해 두었다.

```
https://api.odcloud.kr/api/15043774/v1/uddi:3ad6de21-7ec9-4f46-b2c2-b29f1be09510
  ?page=1&perPage=1000&serviceKey=<DATA_GO_KR_KEY>
```

`DATA_GO_KR_KEY` 를 그대로 쓴다. 즉 **파일을 손으로 내려받아 올릴 필요가 없다.**
중계기 허용 목록에 `api.odcloud.kr` 을 추가해 두었다.

## 남은 한 가지 — 활용신청

첫 호출이 **404** 였다. 이 데이터셋을 아직 활용신청하지 않았기 때문으로 보인다.
(같은 키로 실거래가는 200 이 나오므로 키 자체는 정상이다.)

→ 위 링크에서 `활용신청` 을 누르면 된다. 자동승인.

## 다른 후보들 (참고)

| 번호 | 이름 | 판정 |
|---|---|---|
| **15043774** | 영업소별 교통량 | **채택** — 일자·영업소·차종·하이패스 |
| 15043784 | 분기별 톨게이트 일교통량 | 같은 구조, 분기 단위. 보조 |
| 15062249 | 연평균일교통량 | 구간 단위로 보임. 확인 필요 |
| 15043772 | 통계연보(연도별·월별 차종별) | **노선** 단위라 영업소 분석에 부적합 |
| 15062529 | 영업소 이용차량 | 이용차량 수, 교통량 아님 |
| 15087176 | 영업소 (기준정보) | 좌표·차로수. locationinfoUnit 로 이미 확보 |

## 실시간 API 는 왜 안 되는가

`odtraffic/trafficAmountByUnit` 은 필수 파라미터가 집계시간단위구분코드 + 기준시라
시간 단위 실시간 조회용이다. 과거 연도별 자료를 주지 않는다.
자세한 조사 경과는 `docs/traffic-api-findings.md`.


## 포털 직접 다운로드 (길 B) — 진행 상황

다운로드 화면(`/portal/fdwn/view?type=TCS&num=34&requestfrom=dataset`)을 서울 경유로
읽어 구조를 파악했다.

```
폼 action     /dataset/datasetList/list
입력          dataSupplyDate
선택          collectCycle, dataSupplyYear, dataSupplyMonth,
              dataSupplyQuater, dataSupplyYearQ, dataSupplyYearY
다운로드 후보  /openoasis/portal/download/view2
```

`/openoasis/portal/download/view2` 에 GET 으로 위 파라미터를 붙여 호출하면 **404** 다.
POST 이거나 세션/토큰이 필요한 것으로 보인다. 계속 확인한다.

## 우선순위 정리

분석에 필요한 교통량 범위는 **2015~2025** 이다 (거래가 2016~2025, 1년 시차).
2003년까지 거슬러 갈 필요는 없다.

그리고 **첫 β 검증에는 2~3년이면 충분하다.** 파이프라인이 도는지, 밴드별 계수가
어느 방향으로 나오는지 먼저 보고 나서 전 기간으로 넓히는 것이 순서다.

따라서 지금 필요한 것은:

1. 대표님이 이미 만들어두신 **2025년 1년치 파일** → 파이프라인 검증
2. `15043774` **활용신청** → 오픈API 로 전 기간 자동 수집
3. (예비) 포털 다운로드 자동화 — 1·2 가 되면 필요 없다
