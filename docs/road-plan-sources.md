# 고속도로 공사·신규 IC·계획을 지도에 그리려면

지시(2026-09-16): "현 계획도로처럼 고속도로 공사 및 신규 ic 추가, 계획에
대해서 표시할 수 있는 방법을 찾아주세요. 고속도로 및 고속화 도로는
토지에서 정말 중요한 항목입니다."

## 0. 왜 지금 안 되는가 — 한 줄

지금 그리는 계획도로는 `lt_c_upisuq151`, **도시계획시설 도로**다.
도시계획시설은 **시·군이 결정**하는 것이고 고속국도는 **나라가 놓는다.**
그래서 경부·중부·수도권제2순환 같은 것은 이 층에 아예 안 들어온다.
층을 잘못 고른 것이 아니라 **그 층에 없는 것**이다.

## 1. 브이월드를 다 뒤졌다 — 계획을 담은 층은 없다

브이월드 레이어 177개 중 도로·교통에 걸린 것이 22개였다(vworld-layers
run 14). 그중 계획·공사가 있을 만한 넷을 실제로 불러 속성을 봤다
(`scripts/road_plan_probe.py` · vworld-render run 12). 두드린 자리는
**고속도로 공사가 실제로 있는 곳** 셋이다 — 수도권제2순환(화성 동탄) ·
세종포천(안성 서운) · 서울양평(양평 강상).

| 층 | 온 것 | 계획·공사 칸 |
|---|---|---|
| `lt_l_moctlink` 교통링크(ITS 표준링크) | `rd_rank_h` 에 **고속국도**가 있다 — 고속도로만 가려낼 수는 있다 | **없음** |
| `lt_l_n3a0020000` 도로중심선(수치지도) | `name` 에 '평택-화성고속국도' · '수도권제2순환고속국도' 같은 **노선 이름**이 온다 | **없음** |
| `lt_l_sprd` 도로명주소도로 | 완공 도로만 (대조군대로) | **없음** |
| `lt_c_upisuq151` 도시계획(도로) | 미집행·부분집행·집행완료 단계는 온다 | 있으나 **고속국도가 없다** |

**함정 하나를 피했다.** 도로중심선의 `dvyn` 이 `CSU001/CSU002` 로 와서
'개통/미개통' 인가 싶었는데, 수치지도 표준코드를 확인하니 **분리대 유무**
였다. 코드를 짐작으로 읽었으면 없는 층을 있다고 적을 뻔했다.

그리고 기대했던 샛길 하나도 막혔다 — `pmi_nam` 에 **도시고속도로**라는
값이 있어 고속화도로는 이미 손에 있는 줄 알았는데, 세 자리 모두 **0건**
이었다. 값 목록에 있다고 그 자리에 있는 것은 아니다.

**결론: 지도층으로는 못 한다.** 더 뒤지지 않는다.

## 2. 되는 길 — 사업 자료를 받아 우리가 지도에 얹는다

지도로 오는 자료가 없으면, **글자로 오는 자료에 좌표를 붙이면 된다.**
실거래 지번에 이미 하고 있는 일이고, 지오코더도 그대로 쓴다.

### ★ A. 한국도로공사 「고속도로 공사현황」 — 불러 봤다. 된다

    https://data.ex.co.kr/openapi/safeDriving/hiwayCnstnPrss
    key · type=json · cmcnCstrClssCd (C02 시공 / C03 준공)
      · numOfRows · pageNo · pagingYN

**우리 EX_API_KEY 로 이미 열린다.** 활용신청도 새 키도 필요 없었다
(공공데이터포털 15076874 는 같은 자료를 소개하는 자리고, 실제 길은
도로공사 포털이며 그 호스트는 중계기 ALLOW 에 이미 있다).

실측 (vworld-render run 14 · `scripts/road_work_probe.py`):

| | 건수 |
|---|---|
| **시공 (공사중)** | **233** |
| 준공 (끝남) | 362 |
| 전체 | 595 |

오는 모양은 이렇다 — 공구 단위다.

    평택영월선 · 제천-영월
      명칭 2공구 · 연장 6.300km · 기간 202607~203307 · 준공 20330719
      시점 충청북도 제천시 금성면 동막리
      종점 충청북도 제천시 자작동

**주소 깊이** (99행 × 시점·종점 = 198개):

| 깊이 | 개수 | 비중 |
|---|---|---|
| 리·동 | 118 | 60% |
| **지번** | 55 | 28% |
| 빈칸 | 11 | 5.6% |
| 기타 (측점 `7+780` · `일원` · 괄호) | 14 | 7% |

**88% 가 리·동 이상**이다. 리 단위면 점이 그 리 안에 찍히므로 구간의
끝으로 쓰기에 충분하다.

**검산** — 지오코딩한 두 점 사이 직선거리와 자료의 공사연장을 견줬다.
12건 중 **맞음 5 · 어긋남 1 · 좌표 못 얻음 6**.

    평택영월선 제천-영월    직선 6.0km · 연장 6.3km · 비 0.95  ✓
    평택영월선 제천-영월    직선 6.4km · 연장 6.4km · 비 0.99  ✓
    부산신항-김해          직선 7.1km · 연장 6.9km · 비 1.03  ✓
    부산신항-김해          직선 4.8km · 연장 3.0km · 비 1.62  ✗

**비가 1에 붙는다** — 주소가 맞는 자리를 가리킨다는 뜻이다. 이 검산이
있으면 틀린 구간을 화면에 안 올릴 수 있다.

**좌표 못 얻은 6건은 내 탐침이 틀린 것이다.** 실패한 것들을 보면
'석모로5번길27' · '김포대로 1838-11' 처럼 **도로명주소**인데, 탐침이
`type=PARCEL`(지번)로만 물었다. 고칠 것 셋:

1. 지번으로 물어 실패하면 **도로명으로 다시 묻는다**
2. 측점 표기(`7+780`)·`일원`·짝 안 맞는 괄호를 떼고 묻는다
3. 그래도 안 되면 리·동까지 잘라서 묻는다 (실거래에서 하던 길)

이 셋을 하면 성공률이 크게 오른다. **지금 상태로도 233건 중 절반은
바로 그려진다.**

### ★ B. 「제2차 고속도로 건설계획(2021~2025)」 — **다 읽어 넣었다**

원문을 받았다. 두 가지다.

  관보 제20181호(별권1권) 2022-02-04 — 국토교통부고시 제2022-60호
  계획 본문 68쪽 — 부록 '유형별 고속국도 사업계획표'

둘 다 **글자층이 없는 스캔본**이라 기계로 못 뽑는다. 눈으로 읽어
`data/road/plan2.tsv` 에 옮겼다. **37건 전부다.**

| | 건수 | 연장 | 총사업비 |
|---|---|---|---|
| 신설 중점 | 8 | 323.9km | 215,375억 |
| 신설 일반 | 11 | 446.5km | 250,489억 |
| **신설 계** | **19** | **770.4km** | **465,864억** |
| 확장 중점 | 5 | 83.6km | 19,963억 |
| 확장 일반 | 13 | 265.5km | 64,187억 |
| **확장 계** | **18** | **349.1km** | **84,150억** |

**옮겨 적기가 맞는지는 문서가 스스로 알려 준다.** 고시가 소계와 총계를
같이 적어 뒀고, 우리가 더한 값이 여섯 칸 모두 정확히 맞는다. 한 줄이라도
틀렸으면 안 맞는다. `scripts/test_roadplan.py` 가 매번 다시 검산한다.

본문 부록에는 고시에 없는 칸이 하나 더 있다 — **추진과제 분류**:
①균형 5 · ②혼잡 23 · ③물류 5 · ④미래(남북협력) 4. 확장 18건은 전부
②혼잡이다. 그리고 표 제목이 '[재정]' 이라 **이 37건은 재정사업이고
민자사업은 여기 없다.**

#### 좌표는 절반쯤 이미 붙는다 — 그리고 검산이 틀린 짝을 잡는다

사업명이 `안성JCT-동탄JCT` · `서안성IC-대소JCT` 처럼 **IC·JCT 이름**이라
우리 영업소 명부(좌표 있는 554곳)로 바로 붙는다. 74개 끝점 중 **50개**가
이름만으로 걸린다.

그런데 **이름만 믿으면 안 된다.** 두 끝이 다 붙은 18개 사업에 연장
검산을 걸었더니 11건만 맞았다. 틀린 것들이 이렇다.

    속초-고성      직선 355.7km · 고시 43.5km · 비 8.18  ✗
    창원JCT-진영JCT  직선  14.0km · 고시  4.6km · 비 3.04  ✗

**'고성' 이 강원 고성이 아니라 경남 고성 영업소에 붙었다.** 검산이 없었
으면 강원도에서 경상남도까지 가로지르는 선을 그릴 뻔했다. JCT 가 어긋난
것은 분기점에 영업소가 없어(요금소가 없다) 근처 IC 에 붙은 탓이다.

그래서 남은 일은 이렇다 — ① 시·도 힌트를 붙여 지오코딩(속초-고성 →
강원 고성군), ② JCT 좌표를 따로 구한다(교통노드 `lt_p_moctnode` 에 있을
만하다), ③ **연장 검산을 통과한 것만 화면에 올린다.**

### C. 우리가 이미 가진 것 — 개통 뒤

영업소 명부에 **개통일**이 있고(139개), '그 해에 처음 교통량이 잡힌
영업소 = 신설' 로 신설 IC 를 이미 잡고 있다. 이것이 마지막 단계다.

### 세 자료를 이으면 택지와 같은 모양이 된다

    택지    지구지정 ▸ 개발계획 ▸ 실시계획 ▸ 부분준공 ▸ 준공
    고속도로  건설계획 ▸ 공사중(시공) ▸ 준공 ▸ 개통(영업소)
              └ B ─┘   └──── A ────┘        └─ C ─┘

색과 단계를 계획도로·택지와 **같은 규칙**으로 칠하면 화면에 새 규칙이
안 는다. 이른 단계일수록 진하게, 끝난 것은 기본으로 감춘다.

## 3. 안 되는 것 — 미리 적어 둔다

**노선을 정확한 선으로는 못 그린다.** A 가 주는 것은 시점·종점 **주소**
둘이다. 그 사이를 어떻게 지나가는지는 자료에 없다. 직선으로 이으면
산을 뚫고 가는 그림이 된다.

그래서 이렇게 한다 — **점 두 개와 그 사이를 잇는 굵고 흐린 띠**로
그리고, 화면이 "실제 노선이 아니라 구간의 시작과 끝입니다" 라고 적는다.
좌표 정밀도를 숨기지 않는 것은 실거래 `trade-coarse` 에서 이미 하던
원칙이다.

**진짜 노선 도형을 원하면** 길은 둘인데 둘 다 무겁다.

| 길 | 무엇 | 걸림돌 |
|---|---|---|
| KTDB 장래 네트워크 | 국가교통DB 의 **장래 연도 네트워크**. 개통예정연도가 붙은 링크 | 자료신청 → 승인 필요 (ktdb.go.kr) |
| 도로구역 결정 고시 | 도로법 §25. 수용 대상 땅이 확정되는 **가장 강한 신호** | 고시문+도면(PDF). 기관별로 흩어져 있고 자동화가 어렵다 |

## 4. 사용자가 할 일

### 공사현황(A) — **아무것도 안 해도 된다**

앞서 "공공데이터포털 활용신청이 필요하다" 고 적었는데 **틀렸다.**
실제로 불러 보니 우리 `EX_API_KEY` 로 그냥 열린다. 233건이 이미 손에
있다.

### 계획(B) — 원문 한 번만 봐 주면 된다

「제2차 고속도로 건설계획(2021~2025)」 PDF 를 드라이브 AI 폴더에 넣어
주면 37개 사업(신설 19·확장 18)을 읽어 넣는다. 국토부 정책자료실에
공개돼 있어 받을 수도 있는데, **계획은 개정되므로 어느 판을 쓰는지
화면에 적어야 한다** — 원문을 한 번 확인받는 편이 안전하다.

### 편입 지번(C) — 물어보신 것이 맞다, 다만 길이 둘이다

"고시정보 및 PDF 같은 곳에 고속도로에 포함된 지번 리스트가 들어 있을 수
있습니다" — **맞다.** 도로법 §25 의 「도로구역 결정(변경) 및 지형도면
고시」에 **편입토지조서**가 붙고 거기 지번·지목·면적이 있다. 토지이음
(eum.go.kr)의 고시정보에 건별로 공개돼 있다.

그런데 그것을 긁는 것보다 나은 길이 있다. **고시의 결과는 필지의
토지이용계획에 '도로구역' 으로 찍힌다.** 즉 필지 하나를 물어 '도로구역에
들었는가' 를 바로 알 수 있다 — 고시문을 모으고 지번을 파싱할 필요가 없다.

다만 그 자료는 이미 한 번 막혔다(`api/tile.js` 의 기록): **준보전산지와
접도구역은 브이월드 WFS 에 없고 "국토교통부_토지이용계획정보" 에만 있으며
제공신청이 필요하다.** 도로구역도 같은 자리에 있을 가능성이 높다.

**그래서 순서를 이렇게 제안한다.** 먼저 A 로 구간을 띄우고(오늘 가능),
그것이 쓸모 있으면 그때 토지이용계획정보 제공신청을 한다. 필지 단위
'수용 대상' 표시는 우리 화면에서 가장 강한 신호가 되겠지만, **가장
무거운 신청**이기도 하다 — 쓸모를 먼저 확인하고 신청하는 것이 맞다.

## 5. 제가 할 일 (①이 되면)

1. `road_work` 표를 만든다 — `zone_event`·`rail_open` 과 같은 자리
2. 수집기: 공사현황 API → 시점·종점 주소 지오코딩 → 좌표 두 개
3. 내보내기: `road-work.json` (개통일·단계 포함)
4. 화면: '보일 갈래' 에 **고속도로 공사**를 더하고, 갈래는
   계획 · 공사중 · 준공(기본 꺼짐)
5. 검산: 공사연장(자료)과 두 점 사이 거리(우리 계산)를 견준다 —
   크게 어긋나면 지오코딩이 틀린 것이다

## 6. 재는 자와 기록

    scripts/vworld_layers.py        브이월드에 어떤 층이 있나
    scripts/road_plan_probe.py      그 층에 계획·공사 칸이 있나  → 없다
    scripts/road_build_probe.py     사업 자료에 무엇이 오나      → 다 있다

    scripts/road_work_probe.py      실제로 불러 좌표를 붙여 본다  → 된다

    vworld-layers  run 14   도로·교통 층 22개
    vworld-render  run 12   roads     — 네 층 모두 계획 칸 없음
    vworld-render  run 13   roadbuild — 공사현황 API 항목 확인
    vworld-render  run 14   roadwork  — **시공 233건 · 검산 비 0.95~1.07**
