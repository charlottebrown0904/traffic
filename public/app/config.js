/* 배포 환경별 설정. 이 파일만 바꾸면 앱 코드는 건드리지 않아도 된다.
   Vercel 같은 정적 호스팅에는 매물 API 가 없으므로 apiBase 가 비어 있고,
   앱은 매물 탭을 '준비 중'으로 표시한다. */
window.REDT_CONFIG = {
  // 매물 API 주소. 로컬 개발은 '/api', 정적 배포는 '' (비활성),
  // 나중에 Supabase / 별도 API 서버가 생기면 그 주소를 넣는다.
  apiBase: location.hostname === 'localhost' || location.hostname === '127.0.0.1'
    ? '/api' : '',
  // 프로모션 페이지 주소. 프로모션을 없애면 null 로 바꾸면 '홈으로' 가 사라진다.
  homeUrl: '/',
};

/* 브이월드 **웹 지도용** 키.
 *
 * 채워 넣으면 네이버 지적편집도 같은 용도지역 폴리곤이 배경에 깔린다.
 * 비워 두면 그 배경만 안 나오고 나머지는 그대로 동작한다 — 거래 점은
 * 이미 용도지역 색으로 칠하고 있다.
 *
 * ⚠ 지오코딩에 쓰는 서버 키를 여기 넣지 마세요.
 *
 *   서버 키   하루 3만 건짜리 지오코딩 자원. 중계기 뒤에만 둡니다.
 *             여기 넣으면 브라우저에 그대로 노출되고, 남이 그 한도를
 *             대신 써버리면 우리 수집이 멈춥니다.
 *
 *   웹 지도용 키  브이월드에서 **도메인을 등록해** 따로 발급받습니다
 *             (sado-toji.vercel.app). 등록한 도메인에서만 동작하므로
 *             노출돼도 남이 못 씁니다 — 원래 그렇게 쓰라고 만든 것입니다.
 *
 * 받는 곳: https://www.vworld.kr → 오픈API → 인증키 발급 →
 *          '2D 지도 / 데이터 API', 서비스 URL 에 배포 도메인 입력.
 */
window.VWORLD_WEB_KEY = '';
